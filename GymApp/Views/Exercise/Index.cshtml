@model List<Exercise>

<h1>Ćwiczenia</h1>

<table class="table">
    <thead>
        <tr>
            <th>Nazwa ćwiczenia</th>
            <th>Część ciała</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var exercise in Model)
        {
            <tr>
                <td>@exercise.Name</td>
                <td>@exercise.BodyPart.Name</td>  
            </tr>
        }
    </tbody>
</table>